<!-- item_combo.xml -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Aquí puedes agregar los elementos visuales del combo de salchichas, como nombre, precio, descripción, etc. -->
        <!-- Por ejemplo: -->
        <TextView
            android:id="@+id/comboNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Combo Name"
            android:textStyle="bold"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/comboPriceTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Combo Price"
            android:textSize="16sp" />

        <Button
            android:id="@+id/buttonAddToCart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add to Cart" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
